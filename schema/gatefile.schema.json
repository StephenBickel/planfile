{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/planfile.schema.json",
  "title": "planfile MVP plan artifact",
  "type": "object",
  "required": [
    "version",
    "id",
    "createdAt",
    "source",
    "summary",
    "operations",
    "preconditions",
    "risk",
    "integrity",
    "approval"
  ],
  "properties": {
    "version": { "type": "string" },
    "id": { "type": "string" },
    "createdAt": { "type": "string", "format": "date-time" },
    "source": { "type": "string" },
    "summary": { "type": "string" },
    "dependsOn": {
      "type": "array",
      "items": { "type": "string" }
    },
    "operations": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/operation" }
    },
    "preconditions": {
      "type": "array",
      "items": { "$ref": "#/$defs/precondition" }
    },
    "execution": {
      "type": "object",
      "properties": {
        "commandTimeoutMs": { "type": "number", "exclusiveMinimum": 0 },
        "commandPolicy": { "$ref": "#/$defs/commandPolicy" },
        "filePolicy": { "$ref": "#/$defs/filePolicy" }
      }
    },
    "risk": {
      "type": "object",
      "required": ["score", "level", "reasons"],
      "properties": {
        "score": { "type": "number" },
        "level": { "type": "string", "enum": ["low", "medium", "high"] },
        "reasons": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "integrity": {
      "type": "object",
      "required": ["algorithm", "canonicalizer", "planHash"],
      "properties": {
        "algorithm": { "type": "string", "const": "sha256" },
        "canonicalizer": { "type": "string", "const": "gatefile-v1" },
        "planHash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        }
      }
    },
    "approval": {
      "type": "object",
      "required": ["status"],
      "properties": {
        "status": {
          "type": "string",
          "enum": ["pending", "approved", "rejected"]
        },
        "approvedBy": { "type": "string" },
        "approvedAt": { "type": "string", "format": "date-time" },
        "approvedPlanHash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "attestation": { "$ref": "#/$defs/approvalAttestation" }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "status": { "const": "approved" }
            }
          },
          "then": {
            "required": ["approvedBy", "approvedAt", "approvedPlanHash"]
          }
        }
      ]
    }
  },
  "$defs": {
    "operation": {
      "oneOf": [
        {
          "type": "object",
          "required": ["id", "type", "action", "path"],
          "properties": {
            "id": { "type": "string" },
            "type": { "type": "string", "const": "file" },
            "action": {
              "type": "string",
              "enum": ["create", "update", "delete"]
            },
            "path": { "type": "string" },
            "before": { "type": "string" },
            "after": { "type": "string" }
          }
        },
        {
          "type": "object",
          "required": ["id", "type", "command"],
          "properties": {
            "id": { "type": "string" },
            "type": { "type": "string", "const": "command" },
            "command": { "type": "string" },
            "cwd": { "type": "string" },
            "timeoutMs": { "type": "number", "exclusiveMinimum": 0 },
            "allowFailure": { "type": "boolean" }
          }
        }
      ]
    },
    "commandPolicy": {
      "type": "object",
      "required": ["mode", "patterns"],
      "properties": {
        "mode": { "type": "string", "enum": ["allow", "deny"] },
        "patterns": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "filePolicy": {
      "type": "object",
      "required": ["allowedRoots"],
      "properties": {
        "allowedRoots": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "precondition": {
      "type": "object",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["git_clean", "branch_is", "env_present"]
        },
        "value": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "approvalAttestation": {
      "type": "object",
      "required": ["scheme", "keyId", "publicKeyPem", "payload", "signature"],
      "properties": {
        "scheme": { "type": "string", "const": "ed25519-sha256" },
        "keyId": { "type": "string", "minLength": 8 },
        "publicKeyPem": { "type": "string", "minLength": 32 },
        "signature": { "type": "string", "minLength": 32 },
        "payload": {
          "type": "object",
          "required": ["type", "planId", "approvedBy", "approvedAt", "approvedPlanHash"],
          "properties": {
            "type": { "type": "string", "const": "gatefile-approval-v1" },
            "planId": { "type": "string" },
            "approvedBy": { "type": "string" },
            "approvedAt": { "type": "string", "format": "date-time" },
            "approvedPlanHash": {
              "type": "string",
              "pattern": "^[a-f0-9]{64}$"
            }
          }
        }
      }
    }
  }
}
